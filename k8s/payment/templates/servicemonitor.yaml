apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: payment
  endpoints:
    - port: http
      path: /metrics
